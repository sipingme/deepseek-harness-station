# Station 0.26.9-beta.11

- 同步发布 GitHub 与 GitLab，让仍从 GitHub 检查更新的 beta.8 等旧版本能发现新版。
- 保留 beta.10 的“下载并安装”：下载校验通过后直接启动安装程序并退出 App。
- 安装验证等待卸载子进程完成快捷方式和注册信息清理，避免安装目录先删除导致误报；超时仍判定失败。

维护说明：当前客户端的更新源是 GitHub Release（API 或 Atom），帮助菜单的 GitLab 发布页链接不改变更新源。后续发布需同时向 GitLab 和 GitHub 推送版本标签，并确认两处安装包及校验文件上传成功。
