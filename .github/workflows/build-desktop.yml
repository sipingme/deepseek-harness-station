name: Build desktop installers

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: desktop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  windows:
    name: Windows x64
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm dist:win
      - run: pnpm smoke:installer
      - uses: actions/upload-artifact@v4
        with:
          name: DeepSeek-Harness-Station-Windows-x64
          path: |
            dist/DeepSeek-Harness-Station-*-x64-Setup.exe
            dist/DeepSeek-Harness-Station-*-x64-Setup.exe.blockmap
            dist/SHA256SUMS.txt
          if-no-files-found: error
          retention-days: 14
      - name: Publish GitHub Release assets
        if: startsWith(github.ref, 'refs/tags/v')
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $installer = Get-ChildItem -LiteralPath dist -Filter 'DeepSeek-Harness-Station-*-x64-Setup.exe' | Select-Object -First 1
          $assets = @($installer.FullName, "$($installer.FullName).blockmap", (Resolve-Path 'dist/SHA256SUMS.txt').Path)
          gh release view $env:GITHUB_REF_NAME 2>$null
          if ($LASTEXITCODE -eq 0) {
            gh release upload $env:GITHUB_REF_NAME @assets --clobber
          } else {
            gh release create $env:GITHUB_REF_NAME @assets --verify-tag --generate-notes --prerelease --title "DeepSeek Harness Station $env:GITHUB_REF_NAME"
          }

  macos:
    name: macOS ${{ matrix.arch }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            runner: macos-15-intel
          - arch: arm64
            runner: macos-15
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm dist:mac
      - uses: actions/upload-artifact@v4
        with:
          name: DeepSeek-Harness-Station-macOS-${{ matrix.arch }}
          path: |
            dist/DeepSeek-Harness-Station-*-${{ matrix.arch }}.dmg
            dist/DeepSeek-Harness-Station-*-${{ matrix.arch }}.zip
          if-no-files-found: error
          retention-days: 14
